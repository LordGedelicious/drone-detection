# Aerial Drone Object Detection (Vanilla Architectures)

Prototyping custom single-scale, multi-scale (FPN), and high-resolution (P2) object detection models from scratch using PyTorch.

## Setup
- Environment: PyTorch (RunPod Multi-GPU)
- Custom Objective: Focal Loss + CIoU/MPDIoU
